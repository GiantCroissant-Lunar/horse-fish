{"id":"horse-fish-3aa6","title":"Create hello.py that prints hello world","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T08:41:44.947Z","updatedAt":"2026-03-08T08:42:32.741Z","description":"Create a simple Python file at hello.py that prints Hello from horse-fish","closedAt":"2026-03-08T08:42:32.741Z","closeReason":"Created hello.py that prints 'Hello from horse-fish' as specified"}
{"id":"horse-fish-4b62","title":"Create goodbye.py that prints goodbye world","status":"in_progress","type":"task","priority":2,"createdAt":"2026-03-08T08:43:55.939Z","updatedAt":"2026-03-08T08:43:59.177Z","description":"Create a simple Python file at goodbye.py that prints Goodbye from horse-fish"}
{"id":"horse-fish-ecf3","title":"Create goodbye.py that prints goodbye","status":"in_progress","type":"task","priority":2,"createdAt":"2026-03-08T08:45:54.793Z","updatedAt":"2026-03-08T08:46:00.764Z","description":"Create goodbye.py that prints Goodbye from horse-fish"}
{"id":"horse-fish-84d4","title":"Create greet.py with a greet function","status":"in_progress","type":"task","priority":2,"createdAt":"2026-03-08T08:53:16.004Z","updatedAt":"2026-03-08T08:53:21.145Z","description":"Create greet.py with a function greet(name) that returns Hello name"}
{"id":"horse-fish-5065","title":"Create greet.py with a greet function","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T08:54:40.098Z","updatedAt":"2026-03-08T08:55:35.728Z","description":"Create greet.py with a function greet(name) that returns Hello name","closedAt":"2026-03-08T08:55:35.728Z","closeReason":"Created greet.py with greet(name) function returning 'Hello {name}'"}
{"id":"horse-fish-94f1","title":"Create add.py with an add function","status":"in_progress","type":"task","priority":2,"createdAt":"2026-03-08T08:57:59.567Z","updatedAt":"2026-03-08T08:58:15.685Z","description":"Create add.py with a function add(a, b) that returns a + b"}
{"id":"horse-fish-9ea3","title":"Create multiply.py with a multiply function","status":"open","type":"task","priority":2,"createdAt":"2026-03-08T08:57:59.604Z","updatedAt":"2026-03-08T08:57:59.604Z","description":"Create multiply.py with a function multiply(a, b) that returns a * b"}
{"id":"horse-fish-4cc4","title":"Create add.py with an add function","status":"in_progress","type":"task","priority":2,"createdAt":"2026-03-08T08:59:00.987Z","updatedAt":"2026-03-08T08:59:08.194Z","description":"Create add.py with a function add(a, b) that returns a + b"}
{"id":"horse-fish-0af7","title":"Create add.py with an add function","status":"in_progress","type":"task","priority":2,"createdAt":"2026-03-08T08:59:26.679Z","updatedAt":"2026-03-08T08:59:32.394Z","description":"Create add.py with a function add(a, b) that returns a + b"}
{"id":"horse-fish-4a5b","title":"Create add.py with an add function","status":"in_progress","type":"task","priority":2,"createdAt":"2026-03-08T09:00:10.158Z","updatedAt":"2026-03-08T09:00:25.708Z","description":"Create add.py with a function add(a, b) that returns a + b"}
{"id":"horse-fish-2625","title":"Create add.py","status":"in_progress","type":"task","priority":2,"createdAt":"2026-03-08T09:01:12.891Z","updatedAt":"2026-03-08T09:01:24.891Z","description":"Create add.py with add(a,b) returning a+b"}
{"id":"horse-fish-89eb","title":"Create add.py with an add function","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T09:02:46.393Z","updatedAt":"2026-03-08T09:07:39.420Z","description":"Create add.py with a function add(a, b) that returns a + b","closedAt":"2026-03-08T09:07:39.420Z","closeReason":"Implemented add.py with add(a, b), added validation, and passed required quality gates."}
{"id":"horse-fish-c22e","title":"Create multiply.py with multiply function","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T09:08:08.309Z","updatedAt":"2026-03-08T09:09:39.434Z","description":"Create multiply.py with a function multiply(a, b) that returns a * b","closedAt":"2026-03-08T09:09:39.434Z","closeReason":"Created multiply.py with multiply(a, b) function that returns a * b"}
{"id":"horse-fish-7d17","title":"Pydantic data models for core domain","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T09:18:56.256Z","updatedAt":"2026-03-08T09:24:22.061Z","description":"Create src/horse_fish/models.py with Pydantic v2 models:\n\n1. AgentSlot — id: str, name: str, runtime: str (claude|copilot|pi|opencode), model: str, capability: str (builder|scout|reviewer|lead), state: AgentState (idle|busy|dead), pid: int | None, tmux_session: str | None, worktree_path: str | None, branch: str | None, task_id: str | None, started_at: datetime | None, idle_since: datetime | None\n\n2. AgentState — str enum: idle, busy, dead\n\n3. Subtask — id: str, description: str, agent: str | None, deps: list[str], files_hint: list[str], state: SubtaskState (pending|running|done|failed|escalated), result: SubtaskResult | None\n\n4. SubtaskState — str enum: pending, running, done, failed, escalated\n\n5. SubtaskResult — subtask_id: str, success: bool, output: str, diff: str, duration_seconds: float\n\n6. Run — id: str, task: str, state: RunState (planning|executing|reviewing|merging|completed|failed), subtasks: list[Subtask], created_at: datetime, completed_at: datetime | None\n\n7. RunState — str enum: planning, executing, reviewing, merging, completed, failed\n\nUse Pydantic BaseModel with sensible defaults. Add factory methods Run.create(task) and Subtask.create(description).\n\nAlso create tests/test_models.py with tests for model creation, serialization, state transitions.","closedAt":"2026-03-08T09:24:22.061Z","closeReason":"Created src/horse_fish/models.py with all 7 Pydantic v2 models (AgentSlot, AgentState, Subtask, SubtaskState, SubtaskResult, Run, RunState) and factory methods. 20 tests in tests/test_models.py, all passing, ruff clean."}
{"id":"horse-fish-982d","title":"SQLite store foundation","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T09:19:06.326Z","updatedAt":"2026-03-08T09:29:39.661Z","description":"Create src/horse_fish/store/db.py with a thin SQLite wrapper:\n\n1. class Store:\n   - __init__(self, db_path: str | Path) — opens/creates SQLite DB with WAL mode\n   - execute(sql, params) — execute a query\n   - fetchone(sql, params) — return one row as dict\n   - fetchall(sql, params) — return all rows as list[dict]\n   - close() — close connection\n   - __enter__/__exit__ — context manager\n   - migrate() — run all migrations in order (check schema_version table)\n\n2. Migrations system:\n   - Schema version tracking via a schema_version table\n   - migrations list in db.py with (version: int, sql: str) tuples\n   - Initial migration (v1): create tables for agents, runs, subtasks, mail\n\n3. Tables (v1 migration):\n   - agents: id TEXT PK, name TEXT, runtime TEXT, model TEXT, capability TEXT, state TEXT, pid INTEGER, tmux_session TEXT, worktree_path TEXT, branch TEXT, task_id TEXT, started_at TEXT, idle_since TEXT\n   - runs: id TEXT PK, task TEXT, state TEXT, created_at TEXT, completed_at TEXT\n   - subtasks: id TEXT PK, run_id TEXT FK, description TEXT, agent TEXT, deps TEXT (JSON array), files_hint TEXT (JSON array), state TEXT, result_output TEXT, result_diff TEXT, result_duration REAL, result_success INTEGER\n   - mail: id INTEGER PK AUTOINCREMENT, from_agent TEXT, to_agent TEXT, msg_type TEXT, subject TEXT, body TEXT, created_at TEXT, read INTEGER DEFAULT 0\n\nAlso create tests/test_store.py with tests using tmp_path fixture for in-memory or temp DB.","closedAt":"2026-03-08T09:29:39.661Z","closeReason":"Implemented Store class with WAL SQLite, execute/fetchone/fetchall, context manager, migrate() with schema_version tracking. v1 migration creates agents, runs, subtasks, mail tables. 10 passing tests."}
{"id":"horse-fish-1458","title":"Tmux agent manager","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T09:19:20.359Z","updatedAt":"2026-03-08T09:27:02.842Z","description":"Create src/horse_fish/agents/tmux.py — manages agent processes in tmux panes:\n\n1. class TmuxManager:\n   - async spawn(name: str, command: str, cwd: str, env: dict[str, str] | None = None) -> int\n     Build inline 'export K=V && command' string, run 'tmux new-session -d -s {name} -c {cwd} {wrapped_cmd}'\n     Return PID from 'tmux list-panes -t {name} -F #{pane_pid}'\n   \n   - async send_keys(session_name: str, text: str) -> None\n     Run 'tmux send-keys -t {session_name} {escaped_text} Enter'\n   \n   - async capture_pane(session_name: str) -> str | None\n     Run 'tmux capture-pane -t {session_name} -p', return content or None if session dead\n   \n   - async kill_session(session_name: str) -> None\n     Run 'tmux kill-session -t {session_name}', ignore errors if already dead\n   \n   - async list_sessions() -> list[str]\n     Run 'tmux list-sessions -F #{session_name}', return session names\n   \n   - async is_alive(session_name: str) -> bool\n     Check if session exists in list_sessions\n\nAll methods use asyncio.create_subprocess_exec. Handle edge cases: tmux server not running, session already exists, session died.\n\nAlso create src/horse_fish/agents/runtime.py with:\n\n2. class RuntimeAdapter (Protocol):\n   - runtime_id: str\n   - def build_spawn_command(model: str) -> str\n   - def build_env() -> dict[str, str]\n\n3. class ClaudeRuntime(RuntimeAdapter) — 'claude --print -p' or interactive 'claude'\n4. class CopilotRuntime(RuntimeAdapter) — 'copilot --model {model} --allow-all-tools'\n5. class PiRuntime(RuntimeAdapter) — 'pi --model {model}'\n6. class OpenCodeRuntime(RuntimeAdapter) — 'opencode -m {model}'\n\n7. RUNTIME_REGISTRY: dict[str, RuntimeAdapter] mapping 'claude', 'copilot', 'pi', 'opencode' to instances\n\nCreate tests/test_tmux.py with tests (mock subprocess calls, don't require actual tmux).","closedAt":"2026-03-08T09:27:02.842Z","closeReason":"Implemented tmux manager, runtime adapters, and mocked tests with validation passing."}
{"id":"horse-fish-bbd3","title":"Git worktree manager","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T09:19:30.839Z","updatedAt":"2026-03-08T09:26:29.179Z","description":"Create src/horse_fish/agents/worktree.py — manages git worktrees for agent isolation:\n\n1. class WorktreeManager:\n   - __init__(self, repo_root: str, base_dir: str = '.horse-fish/worktrees')\n   \n   - async create(name: str, base_branch: str = 'main') -> WorktreeInfo\n     Create branch 'horse-fish/{name}' and worktree at '{base_dir}/{name}'\n     Run: git worktree add {path} -b {branch} {base_branch}\n     Return WorktreeInfo(path, branch, name)\n   \n   - async merge(name: str, auto_commit: bool = True) -> bool\n     If auto_commit: stage and commit any uncommitted changes in worktree (with --no-verify)\n     Run: git checkout main && git merge --no-ff {branch}\n     Return True on success, False on conflict\n   \n   - async remove(name: str) -> None\n     Run: git worktree remove {path} --force\n     Run: git branch -D {branch}\n   \n   - async list_worktrees() -> list[WorktreeInfo]\n     Run: git worktree list --porcelain, parse output\n   \n   - async cleanup(max_age_hours: int = 24) -> int\n     Remove worktrees older than max_age_hours\n     Run: git worktree prune\n     Return count of removed worktrees\n   \n   - async get_diff(name: str) -> str\n     Run: git -C {worktree_path} diff HEAD\n     Return diff string\n\n2. WorktreeInfo — Pydantic model: path: str, branch: str, name: str, created_at: datetime\n\nAll git operations via asyncio.create_subprocess_exec. Handle: branch already exists (delete and recreate), worktree path exists (remove first), merge conflicts.\n\nCreate tests/test_worktree.py with tests using tmp_path and git init for isolated test repos.","closedAt":"2026-03-08T09:26:29.179Z","closeReason":"Implemented WorktreeManager with async git operations (create, merge, remove, list, cleanup, diff). 15 tests passing, lint clean."}
{"id":"horse-fish-f512","title":"Build Agent Pool","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T09:42:17.569Z","updatedAt":"2026-03-08T09:45:27.503Z","description":"Create src/horse_fish/agents/pool.py — AgentPool class that wires together TmuxManager, WorktreeManager, RuntimeAdapter (RUNTIME_REGISTRY), and Store to manage agent lifecycle.\n\nAPI methods:\n- spawn(name, runtime, model, capability) -> AgentSlot: lookup runtime in RUNTIME_REGISTRY, create worktree, spawn tmux session with runtime command, persist AgentSlot to store, return slot\n- send_task(agent_id, prompt) -> None: send_keys to agent's tmux session\n- check_status(agent_id) -> AgentState: is_alive check, update state if dead, return state\n- collect_result(agent_id) -> SubtaskResult: capture_pane + get_diff from worktree, build SubtaskResult\n- release(agent_id) -> None: kill tmux session, remove worktree, update store\n- list_agents() -> list[AgentSlot]: fetch from store\n- cleanup() -> int: release dead/idle agents, cleanup old worktrees\n\nTests in tests/test_pool.py — mock tmux/worktree/store, test lifecycle transitions, error handling.\n\nImports: Store from horse_fish.store.db, TmuxManager from horse_fish.agents.tmux, WorktreeManager from horse_fish.agents.worktree, RUNTIME_REGISTRY from horse_fish.agents.runtime, AgentSlot/AgentState/SubtaskResult from horse_fish.models.\n\nConventions: async, Pydantic models, ruff py312 line-length 120, pytest-asyncio.","closedAt":"2026-03-08T09:45:27.503Z","closeReason":"Implemented AgentPool in src/horse_fish/agents/pool.py with all 7 API methods (spawn, send_task, check_status, collect_result, release, list_agents, cleanup). 12 pool tests + 65 total tests pass. Lint clean."}
{"id":"horse-fish-da5c","title":"Build Validation Gates","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T09:42:20.124Z","updatedAt":"2026-03-08T09:47:23.339Z","description":"Create src/horse_fish/validation/gates.py and src/horse_fish/validation/__init__.py — ValidationGates class that runs quality checks against a worktree path before merge.\n\nData model (use dataclass, not Pydantic — keep it lightweight):\n@dataclass\nclass GateResult:\n    gate: str           # 'ruff-check' | 'ruff-format' | 'pytest' | 'compile'\n    passed: bool\n    output: str\n    duration_seconds: float\n\nAPI methods:\n- __init__(gates: list[str] | None = None): default gates = ['compile', 'ruff-check', 'pytest']\n- run_all(worktree_path: str | Path) -> list[GateResult]: run all gates sequentially, return all results (no short-circuit)\n- run_gate(gate: str, worktree_path: str | Path) -> GateResult: run single gate\n- all_passed(results: list[GateResult]) -> bool\n\nGate implementations (each runs subprocess in worktree dir):\n- compile: python -m py_compile on all .py files (glob src/**/*.py tests/**/*.py)\n- ruff-check: ruff check src/ tests/\n- pytest: pytest tests/\n\nTests in tests/test_validation.py — use real tmp_path with sample Python files, test pass/fail for each gate, run_all aggregation, all_passed helper.\n\nConventions: async, ruff py312 line-length 120, pytest-asyncio.","closedAt":"2026-03-08T09:47:23.339Z","closeReason":"Implemented ValidationGates with compile, ruff-check, pytest gates. GateResult dataclass, async subprocess execution, no-short-circuit run_all. 16 new tests, all 69 total passing, lint clean."}
{"id":"horse-fish-ccc4","title":"Build Planner","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T09:42:24.627Z","updatedAt":"2026-03-08T09:45:50.791Z","description":"Create src/horse_fish/planner/decompose.py and src/horse_fish/planner/__init__.py — Planner class that shells out to a runtime CLI to decompose a task into a subtask DAG.\n\nAPI methods:\n- __init__(runtime: str = 'claude', model: str | None = None)\n- decompose(task: str, context: str = '') -> list[Subtask]: build prompt, shell out to CLI (e.g. 'claude --print' for claude runtime), parse JSON response, return list of Subtask objects\n- _build_prompt(task: str, context: str) -> str: system prompt instructing LLM to return JSON array of {description, deps, files_hint}\n- _parse_response(raw: str) -> list[Subtask]: extract JSON from response (handle markdown code fences), validate, create Subtask objects via Subtask.create()\n\nCLI command building per runtime:\n- claude: claude --print -m <model> '<prompt>'\n- copilot: copilot --print --model <model> '<prompt>'\n- pi: pi --print --model <model> '<prompt>'\n- opencode: opencode --print -m <model> '<prompt>'\n\nImport Subtask from horse_fish.models.\n\nTests in tests/test_planner.py — mock asyncio.create_subprocess_exec. Test prompt building, JSON parsing (valid response, malformed JSON, empty response, markdown-fenced JSON), Subtask creation.\n\nConventions: async, ruff py312 line-length 120, pytest-asyncio.","closedAt":"2026-03-08T09:45:50.791Z","closeReason":"Implemented Planner class in src/horse_fish/planner/decompose.py with decompose(), _build_prompt(), _build_command(), _run_cli(), _parse_response(). Tests in tests/test_planner.py (30 passing). Supports claude/copilot/pi/opencode runtimes, JSON parsing with markdown fence handling."}
{"id":"horse-fish-f4a9","title":"Wire Tracer into Orchestrator","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T12:21:12.233Z","updatedAt":"2026-03-08T12:27:47.222Z","description":"Implement Task 1 from docs/plans/2026-03-08-batch4-enhancements.md. Add Tracer as optional param to Orchestrator.__init__(), create trace in run(), wrap each state handler with span/end_span, call end_trace on completion. Wire in cli.py. TDD: write tests first. Use pytest.","closedAt":"2026-03-08T12:27:47.222Z","closeReason":"Wired Tracer into Orchestrator: optional tracer param, span per state handler, end_trace on completion. CLI wired. 218 tests pass."}
{"id":"horse-fish-a5a0","title":"Wire MemoryStore into Orchestrator","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T12:21:13.587Z","updatedAt":"2026-03-08T12:26:02.073Z","description":"Implement Task 2 from docs/plans/2026-03-08-batch4-enhancements.md. Add MemoryStore as optional param to Orchestrator.__init__(), add _learn() method that calls store_run_result() on completion, wire in cli.py. TDD: write tests first. Use pytest.","closedAt":"2026-03-08T12:26:02.073Z","closeReason":"Wired MemoryStore into Orchestrator: added memory param to __init__(), _learn() method calls store_run_result() on completion, wired MemoryStore in cli.py. 218 tests pass."}
{"id":"horse-fish-b563","title":"Stall detection and auto-retry","status":"in_progress","type":"task","priority":2,"createdAt":"2026-03-08T12:21:17.081Z","updatedAt":"2026-03-08T12:22:23.080Z","description":"Implement Task 3 from docs/plans/2026-03-08-batch4-enhancements.md. Add retry_count, max_retries, last_activity_at to Subtask model. Add _check_stalls() to Orchestrator that detects stalled agents by elapsed time, releases them, and retries up to max_retries. Add stall_timeout_seconds param. TDD: write tests first. Use pytest."}
{"id":"horse-fish-451e","title":"Per-state concurrency limits","status":"in_progress","type":"task","priority":2,"createdAt":"2026-03-08T12:21:19.351Z","updatedAt":"2026-03-08T12:22:36.507Z","description":"Implement Task 4 from docs/plans/2026-03-08-batch4-enhancements.md. Add concurrency_limits dict param to Orchestrator.__init__(). In _execute, use per-state limit from dict (falling back to max_agents). TDD: write tests first. Use pytest."}
{"id":"horse-fish-e1bb","title":"Provenance stamping on artifacts","status":"in_progress","type":"task","priority":2,"createdAt":"2026-03-08T12:21:20.318Z","updatedAt":"2026-03-08T12:22:49.937Z","description":"Implement Task 5 from docs/plans/2026-03-08-batch4-enhancements.md. Add agent_id, agent_runtime, agent_model, run_id, completed_at fields to SubtaskResult model (all optional, default None). Add _stamp_provenance() to Orchestrator that stamps these after collecting results. TDD: write tests first. Use pytest."}
{"id":"horse-fish-e004","title":"Stall detection and auto-retry","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T12:31:03.179Z","updatedAt":"2026-03-08T12:38:21.626Z","description":"Implement Task 3 from docs/plans/2026-03-08-batch4-enhancements.md. Add retry_count (default 0), max_retries (default 2), last_activity_at (default None) fields to Subtask model in models.py. Add stall_timeout_seconds param (default 300) to Orchestrator.__init__(). Add _check_stalls() method. Set last_activity_at=datetime.now(UTC) when dispatching and when diff detected. Call _check_stalls() in polling loop. Write tests first with pytest. Run pytest to verify.","closedAt":"2026-03-08T12:38:21.626Z","closeReason":"Implemented stall detection + auto-retry: retry_count/max_retries/last_activity_at on Subtask, stall_timeout_seconds + _check_stalls() on Orchestrator, last_activity_at set on dispatch and diff detection. 221 tests pass."}
{"id":"horse-fish-fd9f","title":"Per-state concurrency limits","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T12:31:05.397Z","updatedAt":"2026-03-08T12:35:11.654Z","description":"Implement Task 4 from docs/plans/2026-03-08-batch4-enhancements.md. Add concurrency_limits: dict[RunState, int] | None = None param to Orchestrator.__init__(). In _execute, use self._concurrency_limits.get(RunState.executing, self._max_agents) instead of self._max_agents. Write tests first with pytest. Run pytest to verify.","closedAt":"2026-03-08T12:35:11.654Z","closeReason":"Implemented per-state concurrency limits: concurrency_limits param on Orchestrator, _execute uses state-specific limit with max_agents fallback. 4 new tests, 222 total passing."}
{"id":"horse-fish-3d32","title":"Provenance stamping on artifacts","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T12:31:07.773Z","updatedAt":"2026-03-08T12:38:33.087Z","description":"Implement Task 5 from docs/plans/2026-03-08-batch4-enhancements.md. Add optional fields to SubtaskResult in models.py: agent_id, agent_runtime, agent_model (all str|None=None), run_id (str|None=None), completed_at (datetime|None=None). Add _stamp_provenance() method to Orchestrator. Call it after setting subtask.result in _execute. Write tests first with pytest. Run pytest to verify.","closedAt":"2026-03-08T12:38:33.087Z","closeReason":"Implemented provenance stamping on SubtaskResult: added 5 optional fields (agent_id, agent_runtime, agent_model, run_id, completed_at) and _stamp_provenance() method to Orchestrator. Tests written first (TDD), 222 tests pass."}
{"id":"horse-fish-e14f","title":"Stall detection and auto-retry","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T12:40:51.122Z","updatedAt":"2026-03-08T12:50:18.084Z","description":"Implement Task 3 from docs/plans/2026-03-08-batch4-enhancements.md. Add retry_count (default 0), max_retries (default 2), last_activity_at (default None) fields to Subtask model in models.py. Add stall_timeout_seconds param (default 300) to Orchestrator.__init__(). Add _check_stalls() method. Set last_activity_at when dispatching and when diff detected. Call _check_stalls() in polling loop. Write tests first with pytest (not bun test). Run pytest to verify.","closedAt":"2026-03-08T12:50:18.084Z","closeReason":"Implemented stall detection and auto-retry: added retry_count/max_retries/last_activity_at to Subtask, stall_timeout_seconds to Orchestrator, _check_stalls() method with agent release and retry logic. All 226 tests pass."}
{"id":"horse-fish-2131","title":"Per-state concurrency limits","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T12:40:53.838Z","updatedAt":"2026-03-08T12:47:11.108Z","description":"Implement Task 4 from docs/plans/2026-03-08-batch4-enhancements.md. Add concurrency_limits: dict[RunState, int] | None = None param to Orchestrator.__init__(). In _execute, use self._concurrency_limits.get(RunState.executing, self._max_agents) instead of self._max_agents. Write tests first with pytest (not bun test). Run pytest to verify.","closedAt":"2026-03-08T12:47:11.108Z","closeReason":"Added per-state concurrency limits: Orchestrator now accepts concurrency_limits dict param; _execute uses state-specific limits instead of global max_agents. Tests: 2 new tests added, all 220 tests pass."}
{"id":"horse-fish-e0b8","title":"Provenance stamping on artifacts","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T12:40:56.192Z","updatedAt":"2026-03-08T12:45:56.601Z","description":"Implement Task 5 from docs/plans/2026-03-08-batch4-enhancements.md. Add optional fields to SubtaskResult in models.py: agent_id, agent_runtime, agent_model (all str|None=None), run_id (str|None=None), completed_at (datetime|None=None). Add _stamp_provenance() method to Orchestrator. Call it after setting subtask.result in _execute. Write tests first with pytest (not bun test). Run pytest to verify.","closedAt":"2026-03-08T12:45:56.601Z","closeReason":"Implemented provenance stamping on artifacts: added agent_id, agent_runtime, agent_model, run_id, completed_at fields to SubtaskResult; added Orchestrator._stamp_provenance() method; called after collecting results in _execute. All 53 tests pass."}
{"id":"horse-fish-b8d2","title":"Provenance stamping on artifacts (glm-5)","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T12:45:02.053Z","updatedAt":"2026-03-08T12:55:17.885Z","description":"Implement Task 5 from docs/plans/2026-03-08-batch4-enhancements.md. Add optional fields to SubtaskResult in models.py: agent_id, agent_runtime, agent_model (all str|None=None), run_id (str|None=None), completed_at (datetime|None=None). Add _stamp_provenance() method to Orchestrator. Call it after setting subtask.result in _execute. Write tests first with pytest (not bun test). Run pytest to verify.","closedAt":"2026-03-08T12:55:17.885Z","closeReason":"Added provenance fields to SubtaskResult (agent_id, agent_runtime, agent_model, run_id, completed_at). Added _stamp_provenance() method to Orchestrator that stamps these fields after setting subtask.result in _execute. All 221 tests pass."}
{"id":"horse-fish-6096","title":"Per-state concurrency limits (codex)","status":"in_progress","type":"task","priority":2,"createdAt":"2026-03-08T12:49:55.316Z","updatedAt":"2026-03-08T12:49:59.108Z","description":"Implement Task 4 from docs/plans/2026-03-08-batch4-enhancements.md. Add concurrency_limits: dict[RunState, int] | None = None param to Orchestrator.__init__(). In _execute, use self._concurrency_limits.get(RunState.executing, self._max_agents) instead of self._max_agents. Write tests first with pytest (not bun test). Run pytest to verify."}
{"id":"horse-fish-fd0b","title":"ID-based deps for blocker-aware dispatch","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T13:12:58.448Z","updatedAt":"2026-03-08T13:18:11.186Z","description":"Switch _deps_met() in orchestrator/engine.py from description-based matching to ID-based matching. Also add _resolve_deps() to convert planner output.\n\nWHAT TO DO:\n1. In src/horse_fish/orchestrator/engine.py, change _deps_met (line ~336) to match by subtask ID instead of description:\n   - Old: done_descriptions = {s.description for s in run.subtasks if s.state == SubtaskState.done}; return all(dep in done_descriptions for dep in subtask.deps)\n   - New: done_ids = {s.id for s in run.subtasks if s.state == SubtaskState.done}; return all(dep in done_ids for dep in subtask.deps)\n\n2. Add a new static method _resolve_deps(subtasks) to Orchestrator class that converts description-based deps to ID-based deps:\n   - Build a dict desc_to_id mapping description->id for all subtasks\n   - For each subtask, replace each dep with desc_to_id[dep] if found, else keep as-is\n   \n3. In _plan() method (line ~157), call self._resolve_deps(subtasks) BEFORE setting run.subtasks = subtasks\n\n4. Update tests in tests/test_orchestrator.py:\n   - Change sample_subtasks fixture: deps=['subtask-1'] instead of deps=['Implement user model']\n   - Change test_execute_respects_dag_deps: deps=['subtask-1'] instead of deps=['Implement base']\n   - Change test_deps_met_all_done: deps=['s1'] instead of deps=['Task 1']\n   - Change test_deps_met_not_done: deps=['s1'] instead of deps=['Task 1']\n   - Change test_deps_met_partial: deps=['s1', 's2'] instead of deps=['Task 1', 'Task 2']\n   - Add test_deps_met_by_id: subtasks with ID-based deps, verify True when done\n   - Add test_resolve_deps_converts_descriptions_to_ids: verify conversion works\n   - Add test_resolve_deps_keeps_unknown_deps: verify unknown deps kept as-is\n\nRun pytest tests/test_orchestrator.py -v to verify all tests pass.","closedAt":"2026-03-08T13:18:11.186Z","closeReason":"Implemented ID-based dependency matching in Orchestrator. Changed _deps_met() to match by subtask ID, added _resolve_deps() to convert planner output from description-based to ID-based deps. Updated tests and added 3 new tests. All 39 tests pass."}
{"id":"horse-fish-b8cb","title":"CLI hf logs command","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T13:13:10.297Z","updatedAt":"2026-03-08T13:16:03.427Z","description":"Add a new 'logs' command to the horse-fish CLI that shows agent tmux output.\n\nWHAT TO DO:\n1. In src/horse_fish/cli.py, add a new command after the merge command:\n\n@main.command()\n@click.option('--agent', default=None, help='Show logs for a specific agent')\n@click.option('--lines', default=20, type=int, help='Number of lines to show per agent')\ndef logs(agent, lines):\n    \"\"\"View agent tmux output.\"\"\"\n    tmux = TmuxManager()\n    if agent:\n        output = asyncio.run(tmux.capture_pane(agent))\n        if output is None:\n            click.echo(f\"Agent '{agent}' not found or no output available.\")\n            return\n        tail = '\\n'.join(output.splitlines()[-lines:]) if output.strip() else '(empty)'\n        click.echo(f'--- {agent} ---')\n        click.echo(tail)\n    else:\n        sessions = asyncio.run(tmux.list_sessions())\n        hf_sessions = [s for s in sessions if s.startswith('hf-')]\n        if not hf_sessions:\n            click.echo('No active horse-fish agents.')\n            return\n        for session in hf_sessions:\n            output = asyncio.run(tmux.capture_pane(session))\n            if output is None:\n                continue\n            tail = '\\n'.join(output.splitlines()[-lines:]) if output.strip() else '(empty)'\n            click.echo(f'--- {session} ---')\n            click.echo(tail)\n            click.echo()\n\n2. Add 4 tests to tests/test_cli.py:\n   - test_logs_lists_all_sessions: mock TmuxManager, list_sessions returns ['hf-agent-1','hf-agent-2','unrelated'], verify only hf- sessions shown\n   - test_logs_single_agent: mock capture_pane, invoke with --agent hf-agent-1, verify output shown\n   - test_logs_agent_not_found: mock capture_pane returns None, verify 'not found' or 'no output' message\n   - test_logs_no_sessions: mock list_sessions returns ['unrelated'], verify 'No active' message\n\nUse from unittest.mock import AsyncMock, MagicMock, patch and @patch('horse_fish.cli.TmuxManager') pattern.\n\nRun pytest tests/test_cli.py -v to verify all tests pass.","closedAt":"2026-03-08T13:16:03.427Z","closeReason":"Added 'hf logs' CLI command with --agent and --lines options to view agent tmux output. Added 4 tests. All quality gates passed."}
{"id":"horse-fish-88c3","title":"End-to-end integration test with real tmux","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T13:13:24.320Z","updatedAt":"2026-03-08T13:18:27.839Z","description":"Create tests/test_e2e.py with real subprocess tests proving tmux + worktree + agent flow works.\n\nWHAT TO DO:\n1. Create tests/test_e2e.py with these contents:\n\n- Import: asyncio, os, subprocess, tempfile, Path, pytest, AsyncMock\n- Import: AgentPool, TmuxManager, WorktreeManager, Subtask, SubtaskState, Store\n- Add _tmux_available() helper that checks subprocess.run(['tmux', '-V'])\n- Add pytestmark = pytest.mark.skipif(not _tmux_available(), reason='tmux not available')\n\n- Fixture tmp_repo(tmp_path): create temp git repo with git init, config user, initial commit of README.md\n- Fixture store(tmp_path): Store(tmp_path/'test.db'), call migrate()\n- Fixture tmux(): return TmuxManager()\n- Fixture worktrees(tmp_repo): return WorktreeManager(str(tmp_repo))\n- Fixture pool(store, tmux, worktrees): return AgentPool(store, tmux, worktrees)\n\nTest 1 - test_e2e_single_subtask_creates_file:\n  - Mark with @pytest.mark.asyncio and @pytest.mark.timeout(30)\n  - Spawn agent via pool.spawn(name='hf-e2e-test', runtime='claude', model='test', capability='builder')\n  - Send shell command via pool._tmux.send_keys that creates a file, git adds, and commits in the worktree\n  - Poll pool.collect_result() in a loop (up to 10 iterations with asyncio.sleep(1))\n  - Assert result.diff is truthy and result.success is True\n  - Cleanup with pool.release(slot.id)\n\nTest 2 - test_e2e_worktree_isolation:\n  - Mark with @pytest.mark.asyncio and @pytest.mark.timeout(30)\n  - Spawn agent via pool.spawn\n  - Assert slot.worktree_path is not None and differs from tmp_repo\n  - Assert Path(slot.worktree_path).exists()\n  - Assert main repo still has README.md\n  - Cleanup with pool.release(slot.id)\n\nRun pytest tests/test_e2e.py -v --timeout=30 to verify tests pass (or skip if no tmux).","closedAt":"2026-03-08T13:18:27.839Z","closeReason":"Created tests/test_e2e.py with e2e integration tests for tmux+worktree+agent flow. 2 tests passing."}
{"id":"horse-fish-0ef7","title":"Ready detection for agent runtimes","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T13:38:46.250Z","updatedAt":"2026-03-08T13:46:05.685Z","description":"Add ready detection so AgentPool waits for the runtime to show its prompt before sending tasks.\n\nWHAT TO DO:\n\n1. Modify src/horse_fish/agents/runtime.py:\n   - Remove frozen=True from all runtime dataclasses (change to just @dataclass(slots=True))\n   - Add ready_pattern and ready_timeout_seconds fields to each runtime:\n     ClaudeRuntime: ready_pattern=r'[❯>]\\s*$', ready_timeout_seconds=30\n     CopilotRuntime: ready_pattern=r'[>]\\s*$', ready_timeout_seconds=60\n     PiRuntime: ready_pattern=r'[>›]\\s*$', ready_timeout_seconds=30\n     OpenCodeRuntime: ready_pattern=r'[>]\\s*$', ready_timeout_seconds=30\n   - Add to RuntimeAdapter protocol: ready_pattern: str, ready_timeout_seconds: int\n   - Update PiRuntime.build_env() to return {'DASHSCOPE_API_KEY': os.environ.get('DASHSCOPE_API_KEY', '')} if key exists, else empty dict. Import os at top.\n\n2. Modify src/horse_fish/agents/pool.py:\n   - Add imports: import asyncio, import re\n   - Add method _wait_for_ready(self, slot: AgentSlot) -> None to AgentPool:\n     - Gets adapter from RUNTIME_REGISTRY[slot.runtime]\n     - Compiles adapter.ready_pattern with re.MULTILINE\n     - Polls self._tmux.capture_pane(slot.tmux_session) every 1s\n     - Returns when pattern matches\n     - If elapsed >= timeout: kill session, remove worktree, raise RuntimeError('Agent ... did not become ready within Ns')\n   - In spawn() method, call await self._wait_for_ready(slot) AFTER creating the AgentSlot but BEFORE the INSERT INTO agents SQL\n\n3. Update tests/test_pool.py:\n   - IMPORTANT: Every existing test that calls pool.spawn() needs tmux.capture_pane mocked to return a string matching the ready pattern. Add tmux.capture_pane = AsyncMock(return_value='Ready\\n❯ \\n') for claude tests, or 'Ready\\n> \\n' for copilot/pi/opencode tests.\n   - For collect_result tests that already mock capture_pane, use side_effect list: first value for ready detection, second for the actual test.\n   - Add 3 new tests:\n     a) test_spawn_waits_for_ready_pattern: mock capture_pane side_effect=['Loading...\\n', 'Loading...\\n❯ \\n'], verify capture_pane called >= 2 times\n     b) test_spawn_raises_on_ready_timeout: mock capture_pane always returns 'Loading...\\n', temporarily set runtime ready_timeout_seconds=2, verify raises RuntimeError matching 'ready'\n     c) test_spawn_works_with_pi_ready_pattern: mock capture_pane returns 'Welcome to Pi\\n> \\n', verify slot created ok\n\nRun pytest tests/test_pool.py -v to verify all tests pass.","closedAt":"2026-03-08T13:46:05.685Z","closeReason":"Added ready detection for agent runtimes: runtime dataclasses now have ready_pattern and ready_timeout_seconds ClassVars, AgentPool._wait_for_ready() polls tmux output before persisting slot, 15 tests pass"}
{"id":"horse-fish-d2d0","title":"Agent prompt template module","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T13:39:03.689Z","updatedAt":"2026-03-08T13:44:27.563Z","description":"Create a prompt template module that wraps task descriptions with project context before sending to agents.\n\nWHAT TO DO:\n\n1. Create src/horse_fish/agents/prompt.py with a single function:\n\ndef build_prompt(task: str, worktree_path: str, branch: str, project_context: str | None = None) -> str:\n    Uses a template that includes:\n    - Intro: 'You are an agent in the horse-fish swarm working in an isolated git worktree.'\n    - Worktree path and branch\n    - Project conventions section (only if project_context is provided)\n    - Task description section\n    - Rules: run pytest, commit when done, stay focused, dont modify files outside scope\n\n2. Modify src/horse_fish/agents/pool.py:\n   - Add import: from horse_fish.agents.prompt import build_prompt\n   - Change AgentPool.__init__ signature to accept optional project_context: str | None = None\n   - Store as self._project_context = project_context\n   - In send_task(), wrap the prompt: full_prompt = build_prompt(task=prompt, worktree_path=slot.worktree_path or '', branch=slot.branch or '', project_context=self._project_context)\n   - Send full_prompt to tmux instead of raw prompt\n\n3. Modify src/horse_fish/cli.py:\n   - In _init_components(), load CLAUDE.md: claude_md = Path.cwd() / 'CLAUDE.md'; project_context = claude_md.read_text() if claude_md.exists() else None\n   - Pass to AgentPool: pool = AgentPool(store, tmux, worktrees, project_context=project_context)\n\n4. Create tests/test_prompt.py with 5 tests:\n   - test_build_prompt_includes_task: verify task text appears in output\n   - test_build_prompt_includes_worktree_info: verify worktree_path and branch in output\n   - test_build_prompt_includes_project_context: pass context='Use ruff for linting.', verify in output\n   - test_build_prompt_works_without_project_context: pass no context, verify no crash\n   - test_build_prompt_includes_rules: verify 'pytest' and 'commit' appear in output\n\n5. Update tests/test_pool.py:\n   - Update test_send_task_sends_keys_and_marks_agent_busy: change assertion from exact match to assert 'implement feature X' in sent_prompt (since prompt is now wrapped)\n   - Add test_send_task_wraps_prompt_with_context: create pool with project_context='Use ruff.', send task, verify both task and context in sent prompt\n\nRun pytest tests/test_prompt.py tests/test_pool.py tests/test_cli.py -v to verify all pass.","closedAt":"2026-03-08T13:44:27.563Z","closeReason":"Created prompt template module (src/horse_fish/agents/prompt.py) with build_prompt function that wraps task descriptions with worktree info, branch, and optional project context. Updated AgentPool to accept project_context and wrap prompts. Updated CLI to load CLAUDE.md and pass as project context. Added 5 tests in tests/test_prompt.py and updated tests/test_pool.py. All 243 tests pass."}
{"id":"horse-fish-b436","title":"Runtime adapter tests","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T13:39:14.075Z","updatedAt":"2026-03-08T13:44:40.123Z","description":"Add comprehensive tests for runtime adapters: ready patterns, Pi env, spawn commands.\n\nWHAT TO DO:\n\n1. Create tests/test_runtime.py with these tests:\n\n- test_all_runtimes_have_ready_pattern: iterate RUNTIME_REGISTRY, assert each has ready_pattern attribute and its not empty\n- test_all_runtimes_have_ready_timeout: iterate RUNTIME_REGISTRY, assert each has ready_timeout_seconds > 0\n- test_claude_spawn_command_includes_model: ClaudeRuntime().build_spawn_command('claude-sonnet-4-6') contains 'claude' and model name\n- test_claude_spawn_command_no_model: ClaudeRuntime().build_spawn_command('') returns 'claude'\n- test_pi_build_env_passes_dashscope_key: with patch.dict(os.environ, {'DASHSCOPE_API_KEY': 'test-key-123'}), PiRuntime().build_env() returns dict with key\n- test_pi_build_env_empty_when_no_key: without DASHSCOPE_API_KEY in env, PiRuntime().build_env() returns {}\n- test_pi_spawn_command: verify 'pi' and model in command\n- test_copilot_spawn_command: verify 'copilot' and '--allow-all-tools' in command\n- test_opencode_spawn_command: verify 'opencode' and '-m' in command\n- test_claude_ready_pattern_matches_prompt: compile ClaudeRuntime().ready_pattern with re.MULTILINE, assert matches '❯ ' and '> ' but not 'Loading...'\n- test_pi_ready_pattern_matches_prompt: compile PiRuntime().ready_pattern with re.MULTILINE, assert matches '> ' and '› ' but not 'Loading Pi...'\n\nImports needed: os, re, pytest, from unittest.mock import patch, and all runtime classes from horse_fish.agents.runtime\n\nRun pytest tests/test_runtime.py -v to verify all tests pass.","closedAt":"2026-03-08T13:44:40.123Z","closeReason":"Added runtime adapter tests and supporting attributes: ready_pattern/ready_timeout_seconds to all runtimes, DASHSCOPE_API_KEY handling to PiRuntime. Created tests/test_runtime.py with 11 comprehensive tests. All 248 tests pass, lint and format pass."}
{"id":"horse-fish-6fad","title":"Add TaskComplexity enum and Run model enhancements","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T15:12:49.558Z","updatedAt":"2026-03-08T15:15:07.958Z","description":"Implement Task 1 from docs/plans/2026-03-08-smart-planner-learning-implementation.md.\n\nAdd TaskComplexity(StrEnum) with values: solo=\"SOLO\", trio=\"TRIO\", squad=\"SQUAD\" to models.py after RunState.\nAdd complexity: TaskComplexity | None = None and lessons: list[str] = Field(default_factory=list) to the Run model.\n\nTDD: Write tests first in tests/test_models.py, verify they fail, implement, verify they pass.\nUse pytest (NOT bun test). Run: pytest tests/test_models.py -v\n\nFiles to modify:\n- src/horse_fish/models.py\n- tests/test_models.py\n\nCommit: git commit -m \"feat: add TaskComplexity enum and Run.complexity/lessons fields\"","closedAt":"2026-03-08T15:15:07.958Z","closeReason":"Added TaskComplexity enum (solo/trio/squad) and Run.complexity/lessons fields. TDD: 32 tests passing."}
{"id":"horse-fish-8638","title":"Add lessons table to SQLite schema","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T15:12:54.874Z","updatedAt":"2026-03-08T15:16:50.463Z","description":"Implement Task 2 from docs/plans/2026-03-08-smart-planner-learning-implementation.md.\n\nAdd migration 2 to MIGRATIONS list in src/horse_fish/store/db.py with:\n- CREATE TABLE lessons (id TEXT PK, run_id TEXT REFS runs, category TEXT NOT NULL, pattern TEXT NOT NULL, content TEXT NOT NULL, task_signature TEXT, created_at TEXT NOT NULL)\n- CREATE INDEX idx_lessons_category ON lessons(category)\n- CREATE INDEX idx_lessons_pattern ON lessons(pattern)\n\nTDD: Write tests first in tests/test_store.py, verify they fail, implement, verify they pass.\nUse pytest (NOT bun test). Run: pytest tests/test_store.py -v\n\nFiles to modify:\n- src/horse_fish/store/db.py\n- tests/test_store.py\n\nCommit: git commit -m \"feat: add lessons table to SQLite schema (migration 2)\"","closedAt":"2026-03-08T15:16:50.463Z","closeReason":"Added lessons table to SQLite schema (migration 2) with category and pattern indexes. TDD: wrote 3 tests first, all 13 tests pass."}
{"id":"horse-fish-3845","title":"Create LessonStore with pattern extraction","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T15:13:04.606Z","updatedAt":"2026-03-08T15:22:52.096Z","description":"Implement Task 3 from docs/plans/2026-03-08-smart-planner-learning-implementation.md.\n\nCreate src/horse_fish/memory/lessons.py with:\n- Lesson(BaseModel): id, run_id, category, pattern, content, task_signature, created_at\n- LessonStore class with:\n  - extract_lessons(run: Run) -> list[Lesson]: detects over_decomposed (subtask_count >> files touched), agent_stalled (retry_count > 0), no_diff (success but empty diff)\n  - store_lesson(lesson: Lesson): INSERT into lessons table\n  - get_lessons_for_task(task, category, limit) -> list[Lesson]: query with word overlap matching\n  - _normalize(text) -> str: lowercase, strip non-alphanumeric\n  - _overlap(sig_a, sig_b) -> bool: check shared words excluding stop words\n\nUses regex r\"diff --git a/(\\S+)\" to extract files from diffs.\nDepends on: horse_fish.models (Run, SubtaskState), horse_fish.store.db (Store)\n\nTDD: Write tests first in tests/test_lessons.py. Use pytest (NOT bun test).\nRun: pytest tests/test_lessons.py -v\n\nFiles to create/modify:\n- src/horse_fish/memory/lessons.py (NEW)\n- tests/test_lessons.py (NEW)\n\nCommit: git commit -m \"feat: add LessonStore with pattern extraction\"","closedAt":"2026-03-08T15:22:52.096Z","closeReason":"Implemented LessonStore with pattern extraction (over_decomposed, agent_stalled, no_diff). Added TaskComplexity enum, Run.complexity/lessons fields, and lessons SQLite migration. 266 tests pass."}
{"id":"horse-fish-a082","title":"Create SmartPlanner with complexity classification","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T15:13:15.466Z","updatedAt":"2026-03-08T15:20:49.187Z","description":"Implement Task 4 from docs/plans/2026-03-08-smart-planner-learning-implementation.md.\n\nCreate src/horse_fish/planner/smart.py with SmartPlanner class:\n- Wraps existing Planner from horse_fish.planner.decompose\n- decompose(task, context) -> tuple[list[Subtask], TaskComplexity]\n- _classify(task, context, lessons) -> TaskComplexity: sends classification prompt to LLM via planner._build_command + planner._run_cli. Prompt asks LLM to reply SOLO/TRIO/SQUAD.\n- _parse_complexity(raw) -> TaskComplexity: parse LLM response, default SOLO if unparseable\n- _strip_ceremony(subtasks) -> list[Subtask]: remove subtasks matching ceremony patterns (commit, test, review, format, lint, verify, etc.)\n- _get_lessons(task) -> str: query LessonStore if available\n- Caps: SOLO=1, TRIO=3, SQUAD=8 max subtasks\n- Fallback: if decompose fails or returns empty or all ceremony -> single subtask wrapping whole task\n\nDepends on: horse_fish.planner.decompose (Planner), horse_fish.memory.lessons (LessonStore), horse_fish.models (Subtask, TaskComplexity)\n\nTDD: Write tests first in tests/test_smart_planner.py. Mock the inner Planner. Use pytest (NOT bun test).\nRun: pytest tests/test_smart_planner.py -v\n\nFiles to create/modify:\n- src/horse_fish/planner/smart.py (NEW)\n- tests/test_smart_planner.py (NEW)\n\nCommit: git commit -m \"feat: add SmartPlanner with complexity classification and ceremony stripping\"","closedAt":"2026-03-08T15:20:49.187Z","closeReason":"Implemented SmartPlanner with complexity classification (SOLO/TRIO/SQUAD), ceremony stripping, subtask capping, and fallback. 25 new tests. All 283 tests passing, lint clean."}
{"id":"horse-fish-974d","title":"Cognee core: CogneeMemory class + tests","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T16:06:36.750Z","updatedAt":"2026-03-08T16:11:46.006Z","description":"Implement Tasks 1-3 from docs/plans/2026-03-09-cognee-integration-implementation.md\n\nREAD the plan first. Follow it exactly.\n\nSummary:\n1. Add cognee to pyproject.toml optional deps under [project.optional-dependencies] memory\n2. Create tests/test_cognee_memory.py with all test classes from the plan\n3. Create src/horse_fish/memory/cognee_store.py with CogneeMemory + CogneeHit classes\n4. Update src/horse_fish/memory/__init__.py to export CogneeMemory + CogneeHit (with try/except ImportError)\n\nIMPORTANT:\n- cognee is already installed (pip install cognee worked)\n- Use pytest for tests. Run: pytest tests/test_cognee_memory.py -v\n- Then run full suite: pytest --tb=short -q (must be 304+ passing)\n- Run ruff check src/ tests/ --fix && ruff format src/ tests/ before committing\n- Commit each logical step separately","closedAt":"2026-03-08T16:11:46.006Z","closeReason":"Implemented Tasks 1-3: cognee added to pyproject.toml memory deps, CogneeMemory class (cognee_store.py) with FastEmbed/LanceDB/Kuzu config and LLM fallback chain, 7 new tests all passing, __init__.py updated with try/except exports. 311 total tests passing."}
{"id":"horse-fish-3a82","title":"Cognee wiring: orchestrator + CLI + SmartPlanner","status":"closed","type":"task","priority":2,"createdAt":"2026-03-08T16:06:48.509Z","updatedAt":"2026-03-08T16:23:27.305Z","description":"Implement Tasks 4-6 from docs/plans/2026-03-09-cognee-integration-implementation.md\n\nREAD the plan first. Follow it exactly.\n\nIMPORTANT: This task depends on horse-fish-974d (CogneeMemory class). That task creates src/horse_fish/memory/cognee_store.py. You MUST have that file available before starting.\n\nSummary:\n1. Task 4: Wire CogneeMemory into Orchestrator\n   - Add cognee_memory param to Orchestrator.__init__ in src/horse_fish/orchestrator/engine.py\n   - Update _learn() to call cognee_memory.ingest_run_result() alongside existing memvid\n   - Use try/except ImportError for the CogneeMemory import\n   - Create tests/test_cognee_orchestrator.py with test classes from plan\n\n2. Task 5: Wire CogneeMemory into CLI\n   - Update src/horse_fish/cli.py _init_components() to create CogneeMemory\n   - Add try/except ImportError for CogneeMemory\n   - Add CLI test to tests/test_cognee_orchestrator.py\n\n3. Task 6: Wire Cognee search into SmartPlanner\n   - Add cognee_memory param to SmartPlanner.__init__ in src/horse_fish/planner/smart.py\n   - Add _get_cognee_context() method\n   - Update decompose() to query Cognee for semantic context\n   - Update Orchestrator SmartPlanner creation to pass cognee_memory\n   - Add SmartPlanner test to tests/test_cognee_orchestrator.py\n\nIMPORTANT:\n- cognee is already installed\n- Use pytest for tests. Run tests after each step.\n- Full suite must pass: pytest --tb=short -q (304+ passing)\n- Run ruff check src/ tests/ --fix && ruff format src/ tests/ before committing\n- Commit each task separately","closedAt":"2026-03-08T16:23:27.305Z","closeReason":"Implemented Tasks 4-6: CogneeMemory wired into Orchestrator._learn(), CLI _init_components(), and SmartPlanner.decompose(). 318 tests passing."}
{"id":"horse-fish-f1a0","title":"Fix retry deadlock in engine.py","status":"closed","type":"task","priority":2,"createdAt":"2026-03-09T01:18:19.000Z","updatedAt":"2026-03-09T01:22:10.186Z","description":"## Bug\n\nIn `src/horse_fish/orchestrator/engine.py`, the `_execute()` method has a retry deadlock.\n\n`_check_stalls()` (line 157) can move a running subtask back to `pending` state and release the agent, but `_execute()` never decrements `active_count` when this happens. At the concurrency limit, a retried task becomes permanently undispatchable because `active_count` still counts the stalled agent.\n\n## Fix Required\n\nIn `_execute()` (line 221), after calling `_check_stalls()` on line 307, subtract the returned retry count from `active_count`:\n\n```python\n# Line 307 currently:\nawait self._check_stalls(run, agent_map)\n\n# Should be:\nretried = await self._check_stalls(run, agent_map)\nactive_count -= retried\n```\n\nAlso: when `_check_stalls` marks a subtask as `failed` (exhausted retries), that also needs to decrement `active_count`. Update `_check_stalls` to track both retried AND failed counts, and return the total count of subtasks that are no longer running.\n\n## Tests\n\nAdd tests in `tests/test_engine.py` that verify:\n1. When a subtask is retried after stall, `active_count` is correctly decremented so new tasks can be dispatched\n2. When a subtask fails after exhausting retries, `active_count` is also decremented\n\nLook at existing tests in `tests/test_engine.py` for patterns — they mock the pool, planner, and gates.\n\n## Constraints\n- Only modify `src/horse_fish/orchestrator/engine.py` and `tests/test_engine.py`\n- Run `pytest tests/test_engine.py -x -q` to verify\n- Run `ruff check src/horse_fish/orchestrator/engine.py` to lint","closedAt":"2026-03-09T01:22:10.186Z","closeReason":"Fixed retry deadlock in engine.py: _check_stalls() returns retried+failed count, _execute() decrements active_count. Added 5 tests. All quality gates passed."}
{"id":"horse-fish-2853","title":"Fix pool.py task identity and cleanup","status":"closed","type":"task","priority":2,"createdAt":"2026-03-09T01:18:33.294Z","updatedAt":"2026-03-09T01:21:39.559Z","description":"## Bug 1: Task identity lost in send_task()\n\nIn `src/horse_fish/agents/pool.py`, `send_task()` (line 90) marks the agent as busy but never persists the `task_id` (subtask_id) in the database. The UPDATE on line 100 only sets `state` but not `task_id`.\n\nThis means `collect_result()` (line 121) uses `slot.task_id or agent_id` which always falls back to `agent_id` since task_id is never set.\n\n### Fix\n\nUpdate `send_task()` to accept and persist the subtask_id:\n\n1. Add a `task_id` parameter to `send_task()`: `async def send_task(self, agent_id: str, prompt: str, task_id: str | None = None) -> None:`\n2. Update the SQL to also set task_id: `UPDATE agents SET state = ?, task_id = ? WHERE id = ?`\n\nThen in `src/horse_fish/orchestrator/engine.py` line 253, pass the subtask.id:\n```python\nawait self._pool.send_task(slot.id, subtask.description, task_id=subtask.id)\n```\n\n**IMPORTANT:** The engine.py change is just adding `task_id=subtask.id` to the existing `send_task` call on line 253. Do NOT modify any other lines in engine.py.\n\n## Bug 2: Incomplete cleanup()\n\n`cleanup()` (line 140) only releases agents in `dead` or `idle` state. Agents that finished work remain `busy` forever, stranding tmux sessions and worktrees. The `hf clean` CLI command promises \"Kill all agents\" but doesn't deliver.\n\n### Fix\n\nChange line 145 from:\n```python\nif slot.state in (AgentState.dead, AgentState.idle):\n```\nto:\n```python\nif slot.state in (AgentState.dead, AgentState.idle, AgentState.busy):\n```\n\nThis makes `cleanup()` release ALL agents regardless of state, which is what \"kill all agents\" should mean.\n\n## Tests\n\nAdd/update tests in `tests/test_pool.py`:\n1. Test that `send_task` persists the task_id in the database\n2. Test that `collect_result` returns the correct subtask_id when task_id is set\n3. Test that `cleanup()` also releases busy agents\n\nLook at existing test patterns in `tests/test_pool.py`.\n\n## Constraints\n- Primary changes in `src/horse_fish/agents/pool.py`\n- Minimal change in `src/horse_fish/orchestrator/engine.py` (only add task_id param to send_task call)\n- Run `pytest tests/test_pool.py -x -q` to verify\n- Run `ruff check src/horse_fish/agents/pool.py` to lint","closedAt":"2026-03-09T01:21:39.559Z","closeReason":"Fixed pool.py task identity and cleanup bugs: send_task() now persists task_id, cleanup() releases busy agents. Added 4 tests. All quality gates pass."}
{"id":"horse-fish-59b0","title":"Add BashRuntime and fix e2e tests","status":"closed","type":"task","priority":2,"createdAt":"2026-03-09T01:18:46.758Z","updatedAt":"2026-03-09T01:21:55.357Z","description":"## Bug\n\nIn `tests/test_e2e.py`, the e2e tests spawn `runtime=\"claude\"` but the docstring says they use \"a bash shell instead of a real agent runtime\". The tests patch `_wait_for_ready` so they don't need claude to actually be ready, but the spawn builds a claude command via the runtime registry. If claude isn't installed, the tmux pane has a dead/error shell.\n\nThe right fix: add a proper BashRuntime adapter so tests can spawn `runtime=\"bash\"`.\n\n## Step 1: Add BashRuntime to runtime.py\n\nIn `src/horse_fish/agents/runtime.py`, add a BashRuntime adapter:\n\n```python\n@dataclass(slots=True)\nclass BashRuntime:\n    \"\"\"Adapter for plain bash shell — used in testing.\"\"\"\n\n    runtime_id: ClassVar[str] = \"bash\"\n    ready_pattern: ClassVar[str] = r\"\\$\\s*$\"\n    ready_timeout_seconds: ClassVar[int] = 5\n\n    def build_spawn_command(self, model: str) -> str:\n        return \"bash\"\n\n    def build_env(self) -> dict[str, str]:\n        return {}\n```\n\nAdd it to RUNTIME_REGISTRY:\n```python\nRUNTIME_REGISTRY: dict[str, RuntimeAdapter] = {\n    ClaudeRuntime.runtime_id: ClaudeRuntime(),\n    CopilotRuntime.runtime_id: CopilotRuntime(),\n    PiRuntime.runtime_id: PiRuntime(),\n    OpenCodeRuntime.runtime_id: OpenCodeRuntime(),\n    BashRuntime.runtime_id: BashRuntime(),\n}\n```\n\n## Step 2: Fix e2e tests\n\nIn `tests/test_e2e.py`:\n\n1. Change both tests from `runtime=\"claude\"` to `runtime=\"bash\"`\n2. The tests can still patch `_wait_for_ready` since the bash prompt pattern might not match in all terminal configs\n3. Update the docstring if needed to match what the test actually does\n\nSpecifically:\n- Line 91: change `runtime=\"claude\"` to `runtime=\"bash\"`\n- Line 129: change `runtime=\"claude\"` to `runtime=\"bash\"`\n\n## Step 3: Add BashRuntime test\n\nIn `tests/test_runtime.py`, add a test for BashRuntime:\n- Test that `build_spawn_command` returns \"bash\"\n- Test that `build_env` returns empty dict\n- Test that it's in RUNTIME_REGISTRY\n\nLook at existing runtime tests for the pattern.\n\n## Constraints\n- Modify `src/horse_fish/agents/runtime.py`, `tests/test_e2e.py`, `tests/test_runtime.py`\n- Do NOT modify pool.py or engine.py\n- Run `pytest tests/test_runtime.py tests/test_e2e.py -x -q` to verify\n- Run `ruff check src/horse_fish/agents/runtime.py` to lint","closedAt":"2026-03-09T01:21:55.357Z","closeReason":"Added BashRuntime adapter to runtime.py with runtime_id='bash', ready_pattern for bash prompt, build_spawn_command returning 'bash'. Updated e2e tests to use runtime='bash' instead of 'claude'. Added comprehensive BashRuntime tests. All 16 tests pass, ruff check and format pass."}
